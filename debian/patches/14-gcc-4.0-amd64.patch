From: Andreas Jochens <aj@andaco.de>
Date: Fri, 14 Jan 2005 10:20:13 +0100
Subject: 14 gcc 4.0 amd64

Fix FTBFS (amd64/gcc-4.0): invalid lvalue in assignment (Bug#290462).
---
 src/standalone.c |    4 ++--
 1 files changed, 2 insertions(+), 2 deletions(-)

diff --git a/src/standalone.c b/src/standalone.c
index a328914..1d01c00 100644
--- a/src/standalone.c
+++ b/src/standalone.c
@@ -76,7 +76,7 @@ void chld_handler(void) {
 	while ((spid = waitpid(0, NULL, WNOHANG)) > 0)
 		for (tmp = 0; tmp < MAX_SESSIONS; tmp++)
 			if (sessions[tmp].pid == spid) {
-				(volatile pid_t) sessions[tmp].pid = 0;
+				sessions[tmp].pid = 0;
 				break;
 			};
 }
@@ -326,7 +326,7 @@ sizeof(ntopbuff)), "::ffff:", 7) == 0) ? ntopbuff + 7 : ntopbuff);
 #else
 				sessions[freeentry].addr = address.sin_addr;
 #endif
-				(volatile pid_t) sessions[freeentry].pid = spid;
+				sessions[freeentry].pid = spid;
 				sessions[freeentry].start = now;
 				sessions[freeentry].log = 0;				
 		};
-- 
